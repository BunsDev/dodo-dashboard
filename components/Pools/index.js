import PoolsTable from "../PoolsTable";

const Pools = () => (
  <>
    <header className="mb-8">
      <h1>Pools</h1>
    </header>

    <div className="card">
      <PoolsTable />
    </div>
  </>
);

export default Pools;
